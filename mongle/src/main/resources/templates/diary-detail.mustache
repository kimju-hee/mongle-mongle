<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>일기 상세 - 감정 일기</title>
    <link href="https://fonts.googleapis.com/css2?family=SUIT:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'SUIT', sans-serif;
            background-color: #fef6f0;
            margin: 0;
        }

        header {
            background: #fff;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0.4rem 2rem;
            box-shadow: 0 2px 6px rgba(0,0,0,0.05);
        }

        header .logo img {
            height: 80px;
        }

        main {
            max-width: 600px;
            margin: 3rem auto;
            padding: 2rem;
            background: white;
            border-radius: 1rem;
            box-shadow: 0 4px 10px rgba(0,0,0,0.08);
        }

        h2 {
            font-size: 1.8rem;
            color: #444;
            margin-bottom: 0.5rem;
        }

        .meta {
            font-size: 0.9rem;
            color: #999;
            margin-bottom: 1.5rem;
        }

        .content {
            font-size: 1rem;
            color: #555;
            line-height: 1.6;
            white-space: pre-line;
        }
    </style>
</head>
<body>
<header>
    <div class="logo">
        <a href="/diarylist"><img src="/images/image.png" alt="몽글몽글"></a>
    </div>
</header>
<main>
    <h2>{{diary.title}}</h2>
    <div class="meta">{{diary.diaryDate}} · 감정: {{diary.emotion.emotionName}}</div>
    <div class="content">{{diary.content}}</div>

    <div style="margin-top: 2rem; display: flex; gap: 1rem;">
        <form action="/diary/{{diary.diaryId}}/edit" method="get">
            <button type="submit" style="padding: 0.5rem 1rem; border-radius: 0.5rem; background: #ffe19b; border: none;">수정</button>
        </form>

        <form action="/diary/{{diary.diaryId}}/delete" method="post" onsubmit="return confirm('정말 삭제하시겠습니까?');">
            <button type="submit" style="padding: 0.5rem 1rem; border-radius: 0.5rem; background: #ff9999; border: none; color: white;">삭제</button>
        </form>
    </div>
</main>
</body>
</html>
