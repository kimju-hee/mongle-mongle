<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>일기 작성 - 감정 일기</title>
    <link href="https://fonts.googleapis.com/css2?family=SUIT:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
          font-family: 'SUIT', sans-serif;
          background-color: #fef6f0;
          margin: 0;
        }
        header {
          background: #fff;
          display: flex;
          align-items: center;
          justify-content: space-between;
          padding: 0.4rem 2rem;
          box-shadow: 0 2px 6px rgba(0,0,0,0.05);
        }
        header .logo {
          display: flex;
          align-items: center;
        }
        header .logo img {
          height: 80px;
          margin-right: 0.75rem;
        }
        nav {
          display: flex;
          justify-content: center;
          align-items: center;
          flex: 1;
        }
        nav a {
          margin-left: 1rem;
          text-decoration: none;
          color: #555;
          font-weight: 500;
          padding: 0.3rem 0.5rem;
          background: linear-gradient(transparent 60%, #fff3b0 60%);
          border-radius: 4px;
        }
        nav a:hover {
          color: #ff8243;
        }
        .user-info {
          display: flex;
          align-items: center;
          gap: 0.6rem;
        }
        .nickname {
          font-weight: 600;
          color: #555;
          white-space: nowrap;
        }
        .logout-btn {
          background-color: #ffc1a1;
          color: white;
          font-weight: 600;
          padding: 0.4rem 0.9rem;
          border: none;
          border-radius: 20px;
          cursor: pointer;
          transition: background-color 0.3s;
        }
        .logout-btn:hover {
          background-color: #ffa577;
        }
        main {
          padding: 2rem;
        }
        h2 {
          text-align: center;
          color: #444;
          margin-bottom: 2rem;
        }
        .diary-list {
          display: flex;
          flex-direction: column;
          gap: 1rem;
          max-width: 600px;
          margin: 0 auto;
        }
        .diary-item {
          background: white;
          padding: 1rem 1.5rem;
          border-radius: 1rem;
          box-shadow: 0 3px 10px rgba(0,0,0,0.05);
        }
        .diary-title {
          font-weight: bold;
          margin-bottom: 0.5rem;
          color: #333;
        }
        .diary-meta {
          font-size: 0.9rem;
          color: #999;
          margin-bottom: 0.5rem;
        }
        .diary-preview {
          color: #666;
        }
    </style>

</head>
<body>
<header>
    <div class="logo">
        <img src="/images/image.png" alt="몽글몽글">
    </div>
    <nav>
        <a href="/write">일기 작성</a>
        <a href="/diarylist">일기 목록</a>
        <a href="/store">스토어</a>
        <a href="/mypayments">구매 목록</a>
        <a href="/emotion/report">감정 리포트</a>
        <a href="/api/user/me">마이페이지</a>
    </nav>
    <div class="user-info">
        <span class="nickname">{{nickname}} 님</span>
        <form action="/logout" method="post">
            <button class="logout-btn" type="submit">로그아웃</button>
        </form>
    </div>
</header>
<main>
    <h2>나의 감정 일기들 📘</h2>
    <div class="diary-list">
        {{#diaryList}}
            <a href="/diary/{{diaryId}}" style="text-decoration: none;">
                <div class="diary-item">
                    <div class="diary-title">{{title}}</div>
                    <div class="diary-meta">{{diaryDate}} · 감정: {{emotion.emotionName}}</div>
                    <div class="diary-preview">{{content}}</div>
                </div>
            </a>
        {{/diaryList}}

        {{^diaryList}}
            <p style="text-align:center; color:#888;">작성한 일기가 아직 없습니다.</p>
        {{/diaryList}}
    </div>

</main>
</body>
</html>
